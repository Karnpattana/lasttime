# บันทึกการทำงาน — tttt.ipynb (DecisionTree ทำนายเกรด)

ไฟล์นี้สรุปว่า `tttt.ipynb` เริ่มต้นเป็นอะไร ถูกแก้/เพิ่ม/ลด/เปลี่ยนอะไรไปบ้างระหว่างคุยกัน และ **เหตุผล** ของแต่ละอย่าง เรียงตามลำดับเวลาที่ทำจริง

---

## 1. จุดเริ่มต้น (ก่อนแก้อะไรเลย)

โน้ตบุ๊กเดิมเป็นโค้ดที่พิมพ์ตาม YouTube สอน มีแค่:

1. โหลด `features04_grade.csv` (720 แถว, 8 ฟีเจอร์: `height_px, length_px, area_px, green_area_px, black_area_px, white_area_px, brown_area_px, green_intensity` + label `grade` เป็น A/B/C)
2. ทำ EDA เบื้องต้น (scatterplot, pairplot)
3. เข้ารหัส `grade` ด้วย `LabelEncoder` (A=0, B=1, C=2)
4. แบ่ง train/test **ตามกลุ่มเลขต้น** (1-10, 11-20, 21-30 สุ่มกลุ่มละ 3 ต้นเป็น test, seed=1) — จุดนี้ทำถูกทางอยู่แล้วตั้งแต่แรก เพราะภาพหลายวันของต้นเดียวกันคล้ายกันมาก ถ้าสุ่มแบ่งทีละแถวจะเกิด data leakage
5. เทรน `DecisionTreeClassifier()` ค่า default ล้วน (ไม่ตั้ง `random_state`, ไม่จำกัด `max_depth`)
6. ดู `.score()` บน train (=1.0) และ test (~0.84-0.85)
7. ลอง `.predict()` ตัวอย่างเดียว 1 แถว

**ปัญหาที่เจอตอนประเมิน**: train accuracy = 1.0 คือ**overfit ชัดเจน** (ต้นไม้จำข้อมูลหมด เพราะไม่จำกัดความลึก), และเพราะไม่ได้ตั้ง `random_state` ผลจึงสุ่มเปลี่ยนได้ทุกครั้งที่รันใหม่ (สังเกตจริง: รันครั้งแรก test acc = 0.8425..., รันอีกครั้ง = 0.8518...) — จุดนี้เป็นข้อจำกัดของโค้ดเดิมที่ไม่ได้แก้ตรงๆ (ไม่แตะโค้ดเดิม) แต่สร้างโมเดลใหม่ที่ตรึง `random_state=1` แทนในส่วนที่เพิ่ม

---

## 2. สิ่งที่เพิ่มเข้าไป (ต่อท้ายไฟล์ ไม่แก้โค้ดเดิม) เรียงตามลำดับ

### 2.1 ตาราง class distribution
**ทำ**: นับจำนวนตัวอย่างแต่ละ grade ทั้งรวม/train/test
**เหตุผล**: ก่อนเชื่อ accuracy ต้องรู้ก่อนว่าข้อมูลสมดุลไหม ถ้า grade ไหนน้อยผิดปกติ accuracy เฉยๆ จะหลอกตาได้
**ผล**: A/B/C = 240/240/240 พอดี สมดุล 100%

### 2.2 Baseline (DummyClassifier แบบทายกลุ่มเยอะสุดเสมอ)
**ทำ**: เทรน `DummyClassifier(strategy='most_frequent')` เทียบ test accuracy กับ DecisionTree เดิม
**เหตุผล**: 0.84 ฟังดูดี แต่ต้องรู้ว่า "ดีกว่าเดาสุ่ม/เดาแบบขี้เกียจแค่ไหน" ถึงจะบอกได้ว่าโมเดลเรียนรู้อะไรจริง
**ผล**: baseline = 0.301, DecisionTree = 0.852 → โมเดลเรียนรู้ได้จริง ไม่ได้ดีเพราะข้อมูลเอนเอียง

### 2.3 ตารางเปรียบเทียบ hyperparameter ด้วย cross-validation
**ทำ**: ลอง `max_depth` (2,3,4,5,6,8,10,None) × `min_samples_leaf` (1,3,5,10) รวม 32 ชุด ใช้ 5-fold cross-validation บน train set เท่านั้น (ไม่แตะ test) แล้วเลือกชุดที่ `cv_mean` สูงสุดมาเป็น `best_model`
**เหตุผล**: โมเดลเดิม default overfit (train=1.0) เพราะไม่จำกัดความลึก การหาค่าที่เหมาะสมต้องใช้ cross-validation บน train เท่านั้น ห้ามดู test ก่อนเลือก ไม่งั้นจะกลายเป็น "แอบดูเฉลย"
**ผล**: ค่าที่ดีที่สุด = `max_depth=4, min_samples_leaf=3` → cv_mean 0.8670, train_acc 0.9286 (ลดจาก 1.0), test_acc 0.8333

**🐛 บั๊กที่เจอและแก้ระหว่างทาง**: ตอนดึง `best_max_depth` จากตาราง เกิด `InvalidParameterError` เพราะ list ค่า `max_depth` มี `None` ปนกับตัวเลข พอสร้างเป็น DataFrame column pandas เลยแปลงทั้งคอลัมน์เป็น `float64` (None → `NaN`, ตัวเลข → `5.0` แทน `5`) แล้ว sklearn ไม่รับ `float` เป็นค่า `max_depth` (ต้องเป็น `int` หรือ `None` เท่านั้น)
**วิธีแก้**: เช็คด้วย `pd.isna()` ก่อน — ถ้าเป็น NaN ให้ใช้ `None`, ถ้าไม่ใช่ให้ `int()` แปลงกลับเป็นจำนวนเต็ม

### 2.4 Confusion matrix + classification report
**ทำ**: ประเมิน `best_model` บน test set แบบละเอียดกว่า accuracy เฉยๆ
**เหตุผล**: accuracy ตัวเดียวไม่บอกว่าโมเดลสับสน grade ไหนกับ grade ไหน ต้องดู precision/recall/f1 แยกราย grade
**ผล**: A แม่นสุด (recall 0.93), C รองลงมา (recall 0.88), **B แม่นน้อยสุด (recall 0.66)** — สับสนกับทั้ง A (10 แถว) และ C (12 แถว)

### 2.5 Feature importance
**ทำ**: ดู `best_model.feature_importances_` เรียงจากมากไปน้อย
**เหตุผล**: DecisionTree จุดเด่นคืออธิบายได้ว่าใช้ฟีเจอร์ไหนตัดสินใจ ควรดูไว้เพื่อเข้าใจโมเดลและหาทางลดฟีเจอร์ที่ไม่จำเป็น
**ผล**: `area_px` (0.788) และ `brown_area_px` (0.124) รวมกัน ~91% ส่วน `green_area_px`, `white_area_px`, `black_area_px` **importance = 0 พอดี** (ไม่ถูกใช้ตัดสินใจแม้แต่ครั้งเดียว)

### 2.6 วาดโครงสร้างต้นไม้ (`plot_tree`)
**ทำ**: วาดกฎการตัดสินใจจริงของ `best_model` เป็นภาพ
**เหตุผล**: เพื่อเห็นว่าโมเดลใช้เงื่อนไขอะไรตัดสินใจจริงๆ ไม่ใช่แค่ดูตัวเลข accuracy

### 2.7 สรุปผลรวม (markdown/print)
**ทำ**: เปรียบเทียบ baseline vs model เดิม (overfit) vs best_model ในที่เดียว พร้อม feature สำคัญสุด 3 อันดับ

---

## 3. การทดลองที่ 2: ตัดฟีเจอร์ที่ importance = 0 ออก

**คำถามจากผู้ใช้**: ลองเอา `green_area_px`, `white_area_px`, `black_area_px` (importance=0) ออกดูว่าผลเปลี่ยนไหม

**ทำ**: สร้าง `X_train_reduced` / `X_test_reduced` ตัด 3 คอลัมน์นั้นออก แล้วรัน cross-validation grid search ชุดเดิม (32 ชุด) ใหม่บนฟีเจอร์ที่เหลือ 5 ตัว ได้ `reduced_model`

**เหตุผล**: ถ้าฟีเจอร์ไม่เคยถูกใช้ตัดสินใจเลย (importance=0) ตัดออกไม่น่าเสียอะไร แต่ต้องพิสูจน์ด้วยการรันจริง ไม่ใช่เดาเอา

**ผล**:

| | best_model (8 ฟีเจอร์) | reduced_model (5 ฟีเจอร์) |
|---|---|---|
| max_depth / min_samples_leaf | 4 / 3 | 4 / 3 (เหมือนเดิม) |
| cv_mean | 0.8670 | **0.8849** (ดีขึ้น) |
| train_acc | 0.9286 | 0.9286 (เท่าเดิม) |
| test_acc | 0.8333 | 0.8333 (เท่าเดิม) |

**วาด `plot_tree` เทียบกัน**: ดึงภาพทั้งสองต้นออกมาดูจริง (decode base64 จากไฟล์ .ipynb) — **โครงสร้างเหมือนกันทุก node เป๊ะ** (เงื่อนไข, gini, samples, class เท่ากันหมด) ยืนยันว่า 3 ฟีเจอร์นั้นไม่มีผลอะไรเลยจริงๆ

**สรุป**: ตัด 3 ฟีเจอร์นี้ออกได้จริง ได้โมเดลเรียบง่ายกว่า (5 ฟีเจอร์แทน 8) โดยไม่เสีย test accuracy และ cross-validation เสถียรขึ้น (cv_std ลดลงด้วย)

---

## 4. การทดลองที่ 3: วินิจฉัยว่าทำไม grade B สับสนบ่อย

**คำถามจากผู้ใช้**: ลองแก้ปัญหา grade B ที่ยังทำนายผิดบ่อย

**แนวทาง**: วินิจฉัยสาเหตุก่อน ยังไม่รีบแก้/เดาทางแก้ (ตามหลัก "เริ่มจากเงื่อนไขง่ายที่สุด/เข้าใจก่อนค่อยเพิ่มความซับซ้อน")

**ทำ 3 อย่าง**:
1. ดึงรายการแถวที่ actual=B แต่ทำนายผิด มาดูค่าฟีเจอร์ตรงๆ
2. ตาราง min/mean/max ของ `area_px`, `brown_area_px` แยกตาม grade เพื่อดูว่าช่วงค่าทับซ้อนกันแค่ไหน
3. scatter plot `area_px` vs `brown_area_px` ไฮไลต์จุดที่ทำนายผิด เทียบตำแหน่งกับ grade อื่น

**ผลที่เจอ**:

| grade | area_px min | mean | max |
|---|---|---|---|
| A | 286,540 | 361,716 | 508,964 |
| B | 236,715 | 286,988 | 345,895 |
| C | 164,819 | 233,959 | 291,254 |

ช่วงของ B **ทับซ้อนกับ A ตั้งแต่ 286,540** และ **ทับซ้อนกับ C ถึง 291,254** — แทบไม่มีโซนที่เป็นของ B ล้วนๆ เลย จุดที่ทำนายผิดก็กระจุกตัวอยู่ตรงรอยต่อ (ใกล้ threshold แรกของต้นไม้ที่ `area_px <= 305411.5` พอดี)

**สรุป**: นี่คือ**ข้อจำกัดตามธรรมชาติของข้อมูล** (grade B เป็นเกรดกลางที่คาบเกี่ยวกับทั้งสองฝั่งจริงๆ) ไม่ใช่บั๊กหรือโมเดลแย่ ต้นไม้แบบ axis-aligned single-threshold ทำได้ดีที่สุดแค่นี้แล้วในสถานการณ์นี้

**ทางเลือกที่เสนอไว้แต่ยังไม่ได้ทำ** (รอผู้ใช้ตัดสินใจ):
1. ยอมรับว่า 0.83 ใกล้เพดานของปัญหานี้แล้ว ไม่ต้องแก้เพิ่ม
2. ลองสร้างฟีเจอร์ผสม เช่น `brown_area_px / area_px` (สัดส่วนแทนค่าดิบ) ดูว่าช่วยแยกโซนก้ำกึ่งได้ไหม
3. เปลี่ยนมุมมองวัดผล — ทาย B ผิดเป็น A/C (เกรดติดกัน) อาจนับว่า "เกือบถูก" แทนที่จะนับผิดเต็มๆ

---

## 5. สรุปภาพรวมตัวเลขสุดท้าย (ณ ตอนนี้)

| รายการ | ค่า |
|---|---|
| Baseline (ทายมั่วๆ) | 0.301 |
| Model เดิม (default, overfit) | train 1.0 / test 0.85 (ไม่คงที่ เพราะไม่ตั้ง random_state) |
| best_model (8 ฟีเจอร์, tuned) | train 0.929 / test 0.833 |
| reduced_model (5 ฟีเจอร์, tuned) | train 0.929 / test 0.833 (ผลเท่า best_model แต่เรียบง่ายกว่า) |
| จุดอ่อนที่เหลือ | grade B (recall 0.66) สับสนกับ A/C เพราะข้อมูลคาบเกี่ยวกันเองตามธรรมชาติ |

**ฟีเจอร์ที่ควรใช้จริง (5 ตัวพอ)**: `height_px, length_px, area_px, brown_area_px, green_intensity`
**ฟีเจอร์ที่ตัดทิ้งได้**: `green_area_px, white_area_px, black_area_px`
**Hyperparameter ที่ดีที่สุด**: `max_depth=4, min_samples_leaf=3`
